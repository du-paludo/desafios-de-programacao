#include <iostream>
#include <vector>
using namespace std;
using ll = long long;

int main() {
    cin.tie(0);
    ios_base::sync_with_stdio(0);

    int n;
    cin >> n;

    ll a;
    vector<ll> array;
  
    for (int i = 0; i < n; i++) {
        cin >> a;
        array.push_back(a);
    }
}